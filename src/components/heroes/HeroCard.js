import React from 'react'

export const HeroCard = (id) => {
    return (
        <div className="col">
            {/*    <div className="card">
                <img src={h.image.url} className="card-img-top" alt="..." />
                <div className="card-body">
                    <h5 className="card-title">{h.name}</h5>
                    <button className="btn btn-primary" onClick={() => { sendInfo(h.id) }}>Agregar</button>
                </div>
    </div>*/}
        </div>
    )
}
